# 内部型号映射说明

供应商原型号不得进入公开网站、静态导出或公开 GitHub 仓库。

2026-09-07 上线前已将 31 条 `ANWELLUP SKU → 供应商原型号` 映射从 `src/catalog.ts` 分离到本机：

`<项目目录>/.private/reference-model-map.json`

`.private/` 已写入 `.gitignore`，不会随网站推送。该文件也包含在上线前的本地项目状态中；后续换电脑或交接采购资料时，需要通过私密渠道单独传递，不能添加到公开仓库。
