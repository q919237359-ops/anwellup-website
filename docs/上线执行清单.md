# ANWELLUP 上线执行清单

按阶段推进；“代码完成”“检查通过”和“视觉验收完成”分别记录，避免把构建成功当成设计定稿。

2026-09-07 需求回查后继续按 `已确认需求与版本依据.md` 推进。Plan A 瓷白 + 绿色体系、正确电话、滚动性能与新版标题字体已进入预览；产品快速详情抽屉、动态材质筛选及原型号前台隐藏已完成代码和导出检查，实际交互仍待验收。

## 当前进度

| 阶段 | 状态 | 本阶段交付 | 进入下一阶段的标准 |
| --- | --- | --- | --- |
| 1. 固定当前版本 | 已完成 | 当前源码与全部 public 素材副本、原 Git HEAD 压缩包、文件校验清单 | 副本完整且可与现有项目独立存放 |
| 2. 首页定稿 | 精修已落地，视觉待验收 | 首页分段对照、产品图片比例修正、动效清理修正 | 桌面与手机实际页面检查通过，用户认可整体方向 |
| 3. 产品页面统一 | 代码完成，待样板验收 | 总览与分类页快速详情抽屉、动态材质筛选、独立详情页、只公开 ANWELLUP SKU | 图片完整、名称可读、筛选与抽屉易用、规格易比较、询盘入口明确 |
| 4. 制造与设备页 | 已有目录图版本，待素材完善 | 生产线、模具、收集设备的图文说明 | 图片与设备对应、来源明确，确认可公开的设备参数与现场信息 |
| 5. 内容与素材核对 | 待进行 | 型号与图片对应、规格和联系方式、文案校对表 | 已有信息准确，待确认字段清楚标示 |
| 6. 手机与交互验收 | 待实际浏览器测试 | 手机、平板、桌面测试记录 | 无重叠或溢出；搜索、筛选、询盘增删、刷新保存和跳转正常 |
| 7. 上线技术整理 | 待进行，已发现部署配置遗留 | 构建产物、历史链接、分享图、加载性能和索引核对 | 构建/路由/部署产物一致，旧链接有明确去向 |
| 8. 测试站 | 待部署 | 可访问测试地址与真实设备验收 | 测试站版本与验收版本一致 |
| 9. 正式上线 | 待测试站完成 | 正式域名、HTTPS、部署与回退记录 | 域名正常、询盘可用、发布版本可追踪 |
| 10. 上线复查 | 待上线 | 错链、加载、移动端与询盘反馈 | 已发现问题闭环，优化项有优先级 |

## 已固定的设计方向

- 使用已提供的新 Logo。网站品牌与产品图片中的印刷内容分别保留。
- 保留原首页的场景图、材质特写和滚动叙事；配色采用已选 Plan A 瓷白 + 鼠尾草绿，搭配新橙色 Logo、衬线展示标题与清晰的无衬线正文。
- 用产品和内容组织页面，减少重复编号、小标题和装饰。
- 首页建立品牌印象；产品页方便比较；制造页以可核实的设备与现场资料说明能力。
- 购物袋原图完整显示，不重新绘制红色印刷，不把低清现场图放大成主视觉。

## 版本与回退

首次基线位于项目旁的 `anwellup-snapshots/2026-09-05T11-56-35-072Z-before-home-review/`。

- `current-source/`：修改前的源码、配置、文档及 public 素材，共 243 个文件，约 28.3 MB。
- `original-head.zip`：原提交 `0eedf5456b0954b48b83d6fdce6bbb971bbb98bc`，用于查看迁移前版本。
- `manifest.json`：每个文件的 SHA-256 校验值、Git 状态和副本信息。
- `RESTORE.md`：在新目录查看和恢复版本的方法。

后续阶段开始前可执行 `node scripts/create-checkpoint.mjs 阶段英文名称` 创建独立快照。脚本不覆盖既有快照；依赖、构建缓存、Git 数据和本地环境文件不复制。

## 上线问题处理记录

2026-09-07 发布前复查：

1. 已将 GitHub Pages 部署产物从旧 `./dist` 修正为 Next.js 的 `./out`。
2. 已移除失效的 `VITE_GTM_ID` / `VITE_CLARITY_ID` 构建变量；当前版本不声称启用统计。
3. 旧版存在 `/about/`、`/samples-ordering/` 等页面，以及 `/#materials`、`/#products`、`/#customization`、`/#process` 锚点。需整理完整映射后决定保留、迁移或跳转。
4. `public/CNAME` 已纳入静态导出，正式域名为 `anwellup.com`；DNS 和 GitHub Pages 实际发布结果在推送后核验。

## 验收记录规则

自动检查记录代码和数据结果。视觉验收记录具体设备尺寸、页面、问题和修正结果；交互验收记录实际操作。没有执行过的检查保持“待完成”。

此前浏览器自动权限检查拒绝了读取 `http://127.0.0.1:4173/`，理由是保存的站点阻止偏好。因此当前没有桌面/手机真实页面截图，也没有 Lighthouse 或浏览器交互通过记录。此限制不影响源码修改、备份和非浏览器检查。
